
<!-- A content heading -->
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<!-- A message -->
<%# messages() %>

<!-- Inventory item detail display -->
<%- detailGrid %>